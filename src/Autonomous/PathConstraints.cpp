class PathContraints
{
public:
    double accerleration;
    double max_velocity;
    double distance;
    PathContraints(double accel, double max_vel, double dist) : accerleration(accel), max_velocity(max_vel), distance(dist) {}
    
};